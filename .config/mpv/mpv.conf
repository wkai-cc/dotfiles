# 硬件加速 (AMD 显卡建议使用 vaapi 或 auto-safe)
hwdec=auto-safe
# 渲染预设：高质量
profile=gpu-hq
# 退出时保存播放进度
save-position-on-quit=yes
# 默认全屏 (根据喜好开启/关闭)
# fs=yes
# 截图路径 (确保 ~/Pictures 存在)
screenshot-directory="~/Pictures"
screenshot-format=png
# 强制使用高对比度字幕
sub-auto=fuzzy
